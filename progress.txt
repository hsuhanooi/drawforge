- Implemented startNewRun with default player state, starter deck, and initial run metadata. Added Jest coverage for initialization and no-combat/no-map selection state.
- Added card model, strike/defend definitions, and a minimal combat engine to execute card effects. Added Jest tests covering card creation and effect execution.
- Verified starter deck composition (5 Strike, 5 Defend) and added tests ensuring it is stored on new runs.
- Added deck zone state (draw/hand/discard) with deterministic shuffle injection, draw/reshuffle/discard behavior, and tests covering draw/removal/reshuffle edge cases.
- Added combat encounter initialization with active state, player-first turn, and basic player/enemy combat state. Added tests for encounter setup.
- Added energy system with turn start initialization and card play energy checks, plus unit tests for spending and rejection on insufficient energy.
- Added card play flow for attack cards: energy payment, effect execution, and hand removal into discard. Added test ensuring Strike reduces enemy health and costs energy.
- Added defense card play coverage: Defend grants block, spends energy, and leaves hand. Added unit test and marked PRD item complete.
- Added damage resolution that consumes block before health and clamps health at zero. Added tests for partial and full block absorption.
- Added next player turn start helper that clears temporary block and refreshes energy. Added tests and marked PRD item complete.
- Added player-to-enemy turn transition helper that switches turn state and starts enemy action phase. Added unit test and marked PRD item complete.
- Added deterministic enemy attack that applies damage and ends enemy turn. Added tests for damage and block interaction, marked PRD item complete.
- Added post-enemy turn flow to refresh energy, reset block, and draw a new hand. Added tests and marked PRD item complete.
- Added combat end-state checks for enemy defeat (victory), wired into card play and enemy actions. Added tests and marked PRD item complete.
- Added test coverage for combat defeat when player health reaches zero and marked PRD item complete.
- Added reward generation for combat victory with deterministic card options and deck addition helper. Added tests and marked PRD item complete.
- Added deterministic map generation with connected rows of combat nodes. Added tests for node counts, types, and connectivity, and marked PRD item complete.
- Added traversal helpers to list valid starting nodes and select a starting node. Added tests for valid selection and rejection of non-starting nodes.
- Added traversal movement helper enforcing connected-node movement. Added tests for valid and invalid moves and marked PRD item complete.
- Added node resolution to start combat encounters on combat nodes. Added tests and marked PRD item complete.
- Added deck inspection helper to list current deck contents and verified reward cards appear in the view. Added tests and marked PRD item complete.
- Added deck removal helper with tests for successful removal and invalid selection, and marked PRD item complete.
- Added save/load support in `src/save.js` with a small storage adapter boundary and JSON-based serialization. Added Jest coverage for saving player/deck/map/combat state, restoring saved runs, malformed JSON, incomplete save data, invalid combat state, and missing saves.
- Added a top-level `README.md` documenting the repo layout, architecture, module guide, and recommended next implementation path.
- Updated `prd.json` to mark the save/load tasks complete after implementation and tests.
- Added balance tuning hooks in `src/balance.js` and wired them into run initialization, turn energy, map generation, combat-node enemy health, reward option counts, and enemy attack damage. Added Jest coverage for default config values and override-based tuning behavior.
- Added a minimal browser shell under `browser/` with New Run / Save / Load / Clear Save controls and a raw run-state viewer backed by `localStorage`. Added `server.js` plus an `npm start` script for local browser testing, and added Jest coverage for browser-demo helper functions.
- Added browser traversal helpers in `src/browserTraversal.js` and wired the browser shell to generate a map, show valid starting/connected nodes, enter nodes, and create combat encounters when moving onto combat nodes. Added Jest coverage for map attachment, valid starting-node selection, connected-node listing, and invalid movement rejection.
- Added browser combat helpers in `src/browserCombat.js` and extended the browser shell with a minimal combat panel showing player/enemy stats, hand rendering, card-play actions, and end-turn resolution into an enemy attack and next player turn. Added Jest coverage for combat startup, card instantiation, indexed hand play, invalid card selection, and end-turn resolution.
- Expanded run content with deterministic enemy variants, elite and boss encounters, event nodes, and a simple relic system. Added core modules for enemies and relics, updated map generation/node resolution/reward logic, and added Jest coverage for enemy selection, relic effects, multi-type map generation, event resolution, and elite/boss reward scaling.
- Upgraded the browser shell from a thin state viewer into a playable test loop: varied node types, event choices, post-combat reward selection, relic tracking, gold progression, boss-win/loss states, and better in-browser run progression. Also added lightweight UI feedback polish through button transitions and status animation.
- Expanded the card/reward pool with `bash`, `barrier`, and `quick_strike`, and updated reward generation to draw from a more varied deterministic set of reward cards.
- Added a simple browser-side card removal flow that can be entered through certain event/reward states, so runs now support practical deck thinning in the test UI rather than only through backend helpers.
- Polished the browser combat/run UX with explicit enemy intent, draw/discard pile counters, reshuffle feedback in status text, and a dedicated end-state panel for victory/defeat so the run outcome is much clearer during manual testing.
- Cleaned up accidental duplicated garbage at the ends of `browser/index.html` and `browser/styles.css` caused by earlier edits, restoring those browser assets to a sane state.
- Added more interesting card effects with browser-usable `focus` (energy utility) and `volley` (attack + draw-style support in the browser model), while keeping the reward pool deterministic and test-covered.
- Added deterministic event variety through `src/events.js` with shrine, forge, and camp-style events instead of a single repeated event pattern. Wired event generation into node resolution and the browser shell.
- Refactored the browser app into a cleaner event/card loop with richer reward-card generation and multiple event outcomes, while keeping save/load and the existing run loop intact.
- Added utility/replacement cards `focus` and `volley`, extended both the browser card library and the core reward pool to include them, and added test coverage for their card definitions/effects.
- Added deterministic multi-template event generation in a dedicated `src/events.js` module and expanded event outcomes to include gold, reward-card offers, and add-card results in addition to healing/relic/removal choices.
- Added enemy behavior variety with deterministic intent sequences for normal, elite, and boss enemies. Enemy actions now support attack, multi-hit attack, block, and buff behaviors instead of every encounter being a flat single-hit attack.
- Updated the browser combat UI to display real readable enemy intent labels and verified through DOM automation that intents advance between turns (for example, Slime rotates from `Slam for 6` to `Harden: gain 6 block`).
- Added visible enemy block to the browser combat panel and fixed the UI wiring so DOM-based testing can read both current intent and current enemy block directly from the page.
- Improved enemy-action feedback in the browser by surfacing the resolved intent label in the status text after enemy turns (for example, after ending a turn against Slime the UI now reports `Slam for 6` instead of a generic message).
- Added a new mechanics layer for cards: exhaust-style one-shots (`surge`, `hex`), temporary energy gain, and a simple debuff interaction where `punish` deals bonus damage against hexed enemies.
- Extended the browser combat UI to track exhaust pile count and enemy hex stacks, and expanded the browser reward/event card pool so the new mechanics can appear in actual runs.
- Added core Jest coverage for advanced card mechanics, including exhaust-pile routing and bonus damage against hexed enemies.
- Fixed a browser rerender bug that made reward selection appear broken: the reward handler was working, but `renderCombat()` crashed because `combatEnemyHex` and `exhaustPileCount` were missing from the DOM elements map. After fixing those bindings, selecting a reward now clears the reward panel and updates the deck/state correctly.
- Fixed a browser draw/reshuffle bug: played cards were not being returned to discard, which caused reshuffle turns to come back with only 4 cards. Restored the correct routing so normal cards go to discard, exhaust cards go to exhaust, end turn discards the remaining hand, and reshuffle turns now refill to a full 5-card hand.
- Added payoff/synergy cards for the newer mechanics: `burnout` (exhaust payoff), `crackdown` (stronger hex payoff), and `momentum` (tempo/energy payoff). Extended both the core reward pool and the browser card library so those cards can appear in runs.
- Added test coverage for the new payoff cards and updated reward-pool tests to reflect the expanded deterministic card set.
- Fixed the browser UI display path for synergy mechanics so `Enemy Hex` and `Exhaust` counters render correctly during injected/browser-driven regression scenarios.
- Added a deterministic DOM synergy regression flow that loads a `Hex` + `Crackdown` hand, verifies hex/exhaust counters change in the browser, and confirms the payoff damage lands as expected.
- Added card rarity ("common"/"uncommon"/"rare") to all card definitions in src/cards.js and the browser CARD_LIBRARY. Updated src/card.js createCard to accept and forward the rarity field. Added 12 new starter-pool cards: Mark of Ruin, Hexblade, Reaper's Clause, Fire Sale, Cremate, Grave Fuel, Brand the Soul, Harvester, Charge Up, Arc Lash, Blood Pact, and Spite Shield. Added combat state fields player.charged (reset each turn) and exhaustedThisTurn (reset each turn). Updated browser playCardAtIndex to handle: costReduceIfHexed, exhaustFromHand, setCharged, drawIfCharged, energyIfCharged, selfDamage, energyPerExhausted, and bonusVsHexedOrExhausted. Updated rewards.js to use a weighted rarity pool (commons 3x, uncommons 2x, rares 1x). Expanded describeCard with rarity and new mechanic text. Added 21 new unit tests across cards.test.js, rewards.test.js, and cardMechanics.test.js. All 265 tests pass and lint is clean.
- Implemented first expansion set of 15 relics (replacing 3) with rarity tiers. Added rarity field to all relics. Updated createRelicReward to use tier-based random selection (tier 1 = common+uncommon, tier 2 = uncommon+rare). Wired all passive relic effects into browser/app.js: Rusted Buckler (combat start block), Quickened Loop (first-turn extra draw), Worn Grimoire (bonus hex on first application), Coal Pendant (draw on first exhaust), Hex Nail (+2 damage vs hexed), Cinder Box (+2 block per exhaust), Volt Shard (block+draw on Charged), Sigil Engine (8 damage when enemy reaches 3+ hex), Time-Locked Seal (first ≤1-cost card free per turn), Phoenix Ash (survive death at 1 HP once per run), Bone Token (+3 heal post-victory), Merchant's Ledger (+1 card reward option). Added phoenix_used: false to startNewRun. Added hasRelic helper. Updated tests: relics.test.js (rarity, tier filtering, pool size), runContent.test.js (elite relic rarity), browserBundle.test.js (static assertions for all wired effects). All 277 tests pass and lint is clean.
- Completed v2 visual UI upgrade for play.html. Rewrote browser/play.js (~550 lines) to wire into the existing rich HTML/CSS structure: visual card components with rarity borders, cost gems, type stripes, and procedural canvas art; combat arena with HP bars (percentage fill with low/mid color transitions), energy pips, hex/block/charged status badges, and enemy intent display; visual map with SVG path lines connecting nodes and state classes (available pulse, current glow, visited dim, locked very dim) with type icons; animations including card fly-out on play, shake on enemy hit, player recoil, enemy lunge, floating damage numbers, turn banner sweep, confetti on win, sparks on defeat; reward screen with large card components and staggered fade-in; relic badge strip with hover tooltips in map and combat; deck inspector overlay; end-state overlay with win/loss theming and run stats. Also: added maxHealth to run.player and maxHp to enemy for HP bar percentages; fixed afterCardSelection to correctly stage the boss relic claim before finishing the node; added buyShopItem server action and /run/buy-shop-item.json endpoint for functional shop purchases. Updated playBundle.test.js assertions to match new api() call pattern. All 159 main project tests pass and lint is clean.
- Milestone 8 Phase 1: Added status effects system — Strength (flat attack bonus), Dexterity (flat block bonus), Vulnerable (+50% damage taken), Weak (-25% damage dealt for player; -25% damage dealt for enemy). All four status effects decay per-turn. Added 8 new cards: war_cry, fortify, expose, cripple, titan_strike, exploit, enfeeble, echo_strike. Added 5 new relics: iron_boots, nimble_cloak, cracked_lens, silencing_stone, warlords_brand. Wired status badges (💪 Strength, 🦶 Dexterity, 😵 Weak for player; 🎯 Vulnerable, 😵 Weak for enemy) in browser combat UI. Added 21 unit tests in statusEffects.test.js. All 180+ tests pass.
- Milestone 8 Phase 2: Added card upgrade system. Created src/cardUpgrade.js with UPGRADE_ID_MAP and upgradeCardInDeck helper. Defined 11 upgraded card variants (strike_plus, defend_plus, bash_plus, barrier_plus, quick_strike_plus, hex_plus, surge_plus, war_cry_plus, fortify_plus, expose_plus, enfeeble_plus). Added upgradeCard(run, deckIndex) server action and /run/upgrade-card.json endpoint. Added "Smith: Upgrade a card" option to campfire event in playContent.js. Added upgrade selection panel to renderRest() in browser/play.js. Added 15 unit tests in cardUpgrade.test.js.
- Milestone 8 Phase 3: Added rest and shop nodes to map generation. buildRowTypeSelections now places exactly 1 rest node and 1 shop node per run in mid-map rows (rows 1 to rows-3). Shop placement prefers a row different from rest to avoid column competition. Updated map.test.js with 2 new tests verifying rest/shop presence and placement constraints.
- Milestone 8 Phase 4: Added Hexbat combat enemy (22 HP, applies Weak 1 on Screech, then attacks for 7) and Stone Sentinel elite (55 HP, Slam 11 → Fortify 8 block → Crush 13). Elites now rotate by (row+col) % 2 for variety. Wired debuff_weak intent type in applyEnemyIntent so enemy Weak application reduces their attack damage. Updated prd.json with Milestone 8 tasks all marked passes: true. All 197 tests pass, lint clean.
- Fixed nodeResolver.js to handle rest and shop node types. Extracted createCampfireEvent() from playContent.js into events.js, added rest/shop cases to resolveNode, updated playContent.js to use the shared helper. Added 2 tests in nodeResolver.test.js.
- Wired combat-start relics in browserCombatActions.js: replaced getEnergyBonus with getCombatEnergyBonus from relics.js (wiring brass_lantern, crown_of_cinders, infernal_battery energy bonuses); wired storm_diadem (start charged), hex_crown (enemy starts with Hex 1), ashen_idol (+1 energy turn 1 only), empty_throne (draw +2 turn 1, draw 4 turn 2), infernal_battery (draw -1 turn 1). Added new per-combat tracking flags (flicker_used, duelist_used_this_turn, grave_wick_used, mirror_used, black_prism_fired, hex_lantern_used, exhaustTotal).
- Wired per-card and damage relics: flicker_charm (first attack +3), duelists_thread (first attack per turn +2), grave_wick (first exhaust costs 0), thorn_crest (deal 3 back when taking damage), furnace_heart (exhaust attack +4), storm_vessel (charged after turn 1 +1 energy), crematorium_bell (first 2 exhausts +1 energy each), black_prism (3 total exhausts +1 energy), hex_lantern (first HP damage → Hex 1). Fixed card routing order so draw effects can reshuffle from discard correctly.
- Wired meta/reward relics in browserPostNodeActions.js and rewards.js: soot_vessel (heal 6 at <=50% HP), bone_token capped at maxHealth, lucky_coin (+5 gold/combat), pilgrims_map (+3 gold non-boss), merchant_ledger/golden_brand (+1 card choice), vault_key (2 boss relic choices via createTieredRelicChoices), leather_thread (+1 maxHealth per 3 wins). Added 30 tests in relicWiring.test.js.
- Implemented 10 new cards: pommel (Dmg 7, Draw 1), brace (Block 7), insight (Draw 2), parry (Block 3 cost 0), heavy_swing (Dmg 12), recover (Block 6, Draw 1), plan_ahead (Draw 2, Exhaust), deep_hex (Hex 3), black_seal (Hex 1, Exhaust, cost 0), feast_on_weakness (Dmg 5, +5 Block if Hexed), malediction (Weak 2, Hex 1). Updated cardRegistry.js (57→60 total, 10 status changes), cardCatalog.js implementedOverrides, cards.js REWARD_POOL. Added 12 tests in newCards.test.js.
- Implemented Powers system: combat.powers[] array, power card routing in playCombatCard, turn_start hooks in endCombatTurn. Added 3 power cards: iron_will (Dex +1/turn), burning_aura (3 dmg/turn), hex_resonance (Hex 1/turn). All powers check for victory condition after firing. Added 10 tests in powers.test.js. Total: 252 tests pass, lint clean.
- Implemented all 24 remaining stub cards (Milestone 10): Charged archetype (static_guard, capacitor, release, guarded_pulse, flashstep), Exhaust archetype (overclock, ashen_blow, final_draft, scorch_nerves, cinder_rush, empty_the_chamber), Hex archetype (curse_spiral, cataclysm_sigil, no_mercy, hexburst), Hex/Exhaust hybrids (soul_rend, doom_engine, unseal, ritual_collapse, doom_bell), and Defense/utility (hollow_ward, refrain, warding_circle, last_word). Added 14 new catalog properties and handlers in browserCombatActions.js: costReduceIfCharged, loseCharged, bonusBlockIfCharged, setChargedIfNotCharged, energyIfExhaustedThisTurn, discardFromHand, exhaustHand, exhaustFromHandCount with hexPerExhausted, exhaustSkillsFromHand, ifHexedExhaustFromHand, ifHexedEnergyGain, activateDoomEngine (turn-scoped hex-on-exhaust), bonusDmgPerHex, bonusDmgPerExhausted, bonusIfLastCard, consumeHexBonus, repeatIfHexed, and returnToHand with returnPile turn tracking. CARD_REGISTRY now has 0 missing cards (60/60 implemented). Added 34 tests in milestone10Cards.test.js. All 286 tests pass, lint clean.
- Night-mode watchdog pass: verified current Drawforge baseline, confirmed full Jest suite passes (48 suites / 345 tests), and fixed eslint failures in `tests/playwright-e2e.js` by declaring the mixed node/browser globals used inside Playwright `page.evaluate()` callbacks and removing stale unused locals. Lint is now clean again.
- Added a card keyword glossary pass in `browser/play.js` and `browser/play.css`: card descriptions and preview text now render interactive keyword terms (Hex, Charged, Exhaust, Draw, Block, Weak, Vulnerable, Strength, Dexterity, Energy, Cost) with shared tooltip copy for quick rules lookup. Added thin-client regression coverage in `tests/playBundle.test.js`, reran the targeted Jest suite, and verified lint stays clean. Marked the card keyword-tooltip task complete in `TODO.md`.
- Broader verification caught a stale assumption in `tests/browserCombatActions.test.js`: the shared combat action can legitimately play a zero-cost card without reducing energy. Updated the test to assert the card leaves hand and lands in discard/exhaust, while only requiring an energy drop for cards that actually cost energy. Re-ran targeted tests, then the full Jest suite (48 suites / 346 tests), and confirmed lint remains clean.
- Added `plans/combat-ui-review-checklist.md` for the Combat UI Visual Direction and UX Foundation milestone. Documented review criteria, scored the combat/reward/deck surfaces, captured the biggest readability gaps, and listed concrete follow-up polish items. This was a documentation-only pass, so no code tests were needed.
- Polished combat pile inspection in `browser/play.html`, `browser/play.js`, and `browser/play.css`: draw/discard/exhaust counters are now explicit interactive buttons with clearer affordance, per-pile aria labels, a richer modal subtitle, and Escape-to-close handling so pile inspection is easier to discover and dismiss during combat. Added thin-client regression coverage in `tests/playBundle.test.js`, reran that Jest suite, and confirmed `npm run lint` stays clean. I also tried to run the Playwright smoke script after updating it with a pile-modal check, but the local environment is currently missing the `playwright` package at runtime, so that browser-level verification is still pending.
- Added clearer combat turn-state presentation in `browser/play.html`, `browser/play.css`, and `browser/play.js`: the center turn indicator now includes a live state chip for player turn, enemy turn, victory, defeat, and relic-trigger messaging; the end-turn control now swaps labels during enemy resolution and resolved combat states; and relic-trigger flashes also surface readable status text without covering the hand. Added thin-client regression coverage in `tests/playBundle.test.js`, reran that Jest suite, and confirmed `npm run lint` stays clean. Marked the turn-controls/turn-state TODO items complete in both active UI milestones.
- Repaired the standalone Playwright smoke in `tests/playwright-e2e.js` so it matches the current UI flows: it now clicks archetype select buttons instead of raw panel containers, re-queries playable hand cards during auto-combat instead of using stale handles, and exits the campfire through the actual leave option before continuing. Verified the smoke end-to-end with `node tests/playwright-e2e.js` and got a clean final report with 0 bugs, 0 console errors, and 0 console warnings.
- Added active hand-card focus states in `browser/play.js` and `browser/play.css`: playable cards are now keyboard-focusable buttons, Enter/Space can play the focused card, and the current hover/focus target gets a persistent `is-active` presentation so only one card is visually elevated at a time. Added thin-client regression coverage in `tests/playBundle.test.js`, reran that suite, and verified lint stays clean.
- Added thin-client combat UI regression coverage in `tests/playBundle.test.js` for the dedicated player HUD, enemy intent panel, combat layout regions, and relic tooltip/trigger wiring. Re-verified those browser presentation hooks, marked the player-HUD and enemy-intent TODO items complete in the Combat UI Visual Direction and UX Foundation milestone, and kept the verification scoped to Jest + lint.
- Follow-up watchdog pass: validated the new combat-layout regression checks with `npm test -- --runInBand tests/playBundle.test.js` and `npm run lint`, then synced the overlapping TODO items in both UI milestones so the completed combat layout, player HUD, and enemy intent/presentation tasks are now marked done consistently.
- Added another thin-client regression assertion for the shared `makeCard()` renderer in `tests/playBundle.test.js`, covering rarity/type styling, art/cost/name/rules content, and reuse across hand, reward, deck, and removal surfaces. Re-ran the targeted Jest suite plus lint, then marked the reusable upgraded card-component tasks complete in both active UI milestones.
- Tightened the combat hand presentation in `browser/play.js` and `browser/play.css` with an adaptive arced layout: dense hands now compress with controlled overlap, preserve readable fan spacing from 3 to 10 cards, and keep hovered cards rising cleanly above neighbors. Added thin-client regression coverage in `tests/playBundle.test.js`, reran that Jest suite, and confirmed `npm run lint` stays clean. Marked the arced hand-layout TODO item complete.
- Synced the active-card hover/focus task in `TODO.md` with the already-landed implementation: the hand now uses a single `is-active` card state for hover/focus clarity, keyboard-playable buttons, and focused-card elevation, and that behavior is covered by `tests/playBundle.test.js`. Re-ran the targeted thin-client Jest suite plus lint and kept both green.
- Added click-to-select and drag-up-to-play hand interactions in `browser/play.js` and `browser/play.css`: playable cards now enter a pending selected state on first click or pointer-down, can be confirmed with a second click, and resolve immediately when dragged upward past a release threshold. Added thin-client regression coverage in `tests/playBundle.test.js`, reran that Jest suite, and confirmed `npm run lint` stays clean. Marked the click/drag card-play tasks complete in both active UI milestones.
- Extended the same hand-selection flow with explicit enemy targeting and live outcome previews: selected attack cards now highlight the enemy panel and require enemy confirmation before resolving, while selected cards also surface preview chips for expected damage, block, energy, draw, and status application. Added thin-client regression coverage in `tests/playBundle.test.js`, reran that Jest suite, and confirmed `npm run lint` stays clean. Marked the targeting and card-preview TODO items complete in both active UI milestones.
- Added shared ordered card-effect pipeline support in `src/combatEngine.js` plus structured conditional branches and structured keyword metadata in `src/card.js` / `src/cardCatalog.js`, so cards can now describe multi-step effects, deterministic hex/charged conditionals, and renderer-friendly keyword data without bespoke one-off handlers. Added regression coverage in `tests/cards.test.js`, re-ran `tests/cards.test.js`, `tests/cardMechanics.test.js`, `tests/cardCatalog.test.js`, `tests/playBundle.test.js`, and `npm run lint`, then marked the ordered-effects, conditional-logic, and keyword-metadata TODO items complete.
- Added a shared combat delta-feedback pass in `browser/play.js` and `browser/play.css`: card plays and turn transitions now surface floating feedback for block gains, energy changes, card draw, enemy status application, player Charged gain, and relic follow-up messaging instead of only damage. Added thin-client regression coverage in `tests/playBundle.test.js`, re-ran that suite plus `npm run lint`, and marked the combat-feedback tasks complete in both active UI milestones. I also re-ran the Playwright smoke; it still hits the older combat-flow issue where a played Defend leaves hand/discard counts unchanged, so that browser-level regression is the next active TODO to investigate.
- Watchdog resume pass: fixed live combat targeting wiring so selecting an attack card now immediately updates enemy targeting state instead of waiting for a full rerender, and added explicit click/keyboard thin-client coverage for the hand-card activation path. Updated the Playwright smoke to account for the newer select-then-confirm combat flow, then re-ran `tests/playBundle.test.js`, `tests/cards.test.js`, `npm run lint`, and `node tests/playwright-e2e.js`. Result: the earlier "card leaves hand" regression is fixed, but the smoke still surfaces two later blockers to resume next: the `1` keyboard shortcut path can fail to resolve a confirmed play in some combat states, and the auto-combat smoke loop can stall without spending cards, leaving combat unwon after 15 turns.
- Watchdog finish-current-chunk pass: stabilized the remaining combat smoke regressions by adding explicit card-type data to the shared card renderer, routing numeric hand shortcuts through a deterministic combat helper, and updating the Playwright smoke to pick a stronger deck and prioritize damaging plays. Also implemented the next TODO item, a lightweight combat log: combat state now keeps a capped recent-action log for combat start, card resolutions, enemy actions, relic-trigger beats, and defeat/victory updates, and the browser combat screen now renders that log in a dedicated panel. Verified with `npm test -- --runInBand tests/browserCombatActions.test.js tests/playBundle.test.js`, `npm test -- --runInBand tests/cards.test.js`, `npm run lint`, and `node tests/playwright-e2e.js`.
- Watchdog follow-through pass: tightened the next two UI TODO items instead of letting them drift. Added explicit regression coverage that reward cards are rendered via the shared large `makeCard()` flow with the same click/keyboard claim interaction, and extended the deck overlay with type filters on top of the existing sorting so larger decks now have a real browsing aid beyond raw scrolling. Re-verified with `npm test -- --runInBand tests/playBundle.test.js tests/browserCombatActions.test.js tests/cards.test.js`, `npm run lint`, and `node tests/playwright-e2e.js`, then marked both the post-combat reward-card presentation task and the deck-inspection filtering task complete in `TODO.md`.
- Continued immediately into the next unfinished relic-UI work instead of stopping: relic badges are now focusable inspection controls with richer tooltip metadata (rarity plus passive/triggered/start-of-combat treatment), both the map and combat HUD expose a relic overlay listing all acquired relics, and triggered relic flashes temporarily promote their tooltip state for clearer feedback. Added thin-client regression coverage for the new relic overlay controls, reran `npm test -- --runInBand tests/browserCombatActions.test.js tests/playBundle.test.js`, `npm run lint`, and `node tests/playwright-e2e.js`, then marked the overlapping relic-inspection TODO items complete in both active UI milestones.
- Kept moving into the next unfinished UX-foundation item: added `plans/combat-wireframes.md` with a low-fidelity combat-screen wireframe, region rules, density checks for 3 to 10 cards and larger relic counts, and the approved baseline composition for future art/presentation work. This was a documentation pass, so no code tests were needed, and the wireframe task is now marked complete in `TODO.md`.
- Watchdog verification pass: ran the full local Jest suite (`48 suites / 366 tests`) and `npm run lint` after the latest UI milestone slice, both green. Added `.github/workflows/ci.yml` so GitHub Actions now runs the same test + lint checks on pushes to `main` and pull requests, and documented that CI expectation in `README.md`. The remaining CI TODO is only the remote confirmation step once the workflow runs on GitHub.
- Watchdog follow-through pass: documented the original Drawforge visual direction in `plans/visual-style-guide.md` and the shared card anatomy in `plans/card-frame-system.md`, covering palette, typography, iconography, rarity/type treatments, and state/readability rules for implementation. This was a documentation slice, so no code tests were needed beyond the already-green full-suite + CI verification from the same pass.
- TODO sync pass: marked the already-landed hand hover/focus/inspection and targeted-card highlight/preview UX tasks complete in the Combat UI Visual Direction milestone so the checklist now matches the implemented browser behavior and existing regression coverage.
- Watchdog resume pass: completed the interrupted relic-definition migration by making `src/relicRegistry.js` the canonical data-driven relic source with shared rarity, tier, rules text, trigger type, and asset references for all 44 relics. Verified instantiation and shared-hook compatibility with `npm test -- --runInBand tests/relicRegistry.test.js tests/relics.test.js` and `npm run lint -- src/relicRegistry.js tests/relicRegistry.test.js tests/relics.test.js`, then marked the relic-definition TODO item complete.
- Resumed Night Mode on the next real implementation slice and collapsed the relic split-brain: `src/relicRegistry.js` is now the canonical relic definition source for all 44 relics, including rarity, tier, trigger type, asset reference, and rules text, and `src/relics.js` now derives the live reward catalog from that registry instead of maintaining a second drifting copy. Added regression coverage in `tests/relicRegistry.test.js` and `tests/relics.test.js`, then re-ran `tests/relicRegistry.test.js`, `tests/relics.test.js`, `tests/relicWiring.test.js`, and `npm run lint`, all green.
- Continued directly into the next unfinished art-pipeline slice instead of stopping after the relic registry task. Added a shared `src/assets.js` presentation-asset helper with category validation, convention-based refs, and safe placeholders for cards, relics, enemies, icons, backgrounds, and VFX. Wired renderable cards, relics, and enemies to expose presentation metadata and added regression coverage in `tests/assets.test.js`, `tests/cards.test.js`, `tests/relics.test.js`, and `tests/enemies.test.js`. Verified with `npm test -- --runInBand tests/assets.test.js tests/cards.test.js tests/relics.test.js tests/enemies.test.js` and `npm run lint -- src/assets.js src/cardCatalog.js src/relics.js src/enemies.js tests/assets.test.js tests/cards.test.js tests/relics.test.js tests/enemies.test.js`.
- Watchdog resume pass: finished the data-driven card-definition migration by collapsing `src/cards.js` onto the shared card catalog/runtime metadata instead of hand-maintained per-card factories, while keeping the existing factory exports stable for consumers. Also synced the already-landed keyword/glossary tooltip task in `TODO.md`. Verified with `npm test -- --runInBand tests/browserCombat.test.js tests/cards.test.js tests/cardMechanics.test.js tests/rewards.test.js tests/attackCard.test.js tests/defenseCard.test.js tests/energy.test.js tests/deckView.test.js tests/newCards.test.js` and `npm run lint -- src/cards.js src/card.js src/browserCombat.js src/deck.js src/rewards.js tests/browserCombat.test.js tests/cards.test.js tests/cardMechanics.test.js tests/rewards.test.js tests/attackCard.test.js tests/defenseCard.test.js tests/energy.test.js tests/deckView.test.js tests/newCards.test.js`.
- Follow-through TODO sync: reviewed the already-landed shared asset pipeline against both active UI milestones and marked the overlapping art-pipeline tasks complete in `TODO.md`. Existing coverage already verifies category validation, placeholder fallback behavior, and presentation-asset refs for cards, relics, enemies, icons, backgrounds, and VFX in `tests/assets.test.js`, `tests/cards.test.js`, `tests/relics.test.js`, and `tests/enemies.test.js`, so no new code changes or extra verification were needed for this checklist sync.
- Added theme-aware browser rendering for Drawforge's next unfinished UI slice: archetype-driven surface themes now flow through map/combat/reward/shop/rest screens, shared cards/preview popups expose frame-variant data (`neutral`, `hex`, `ember`, `storm`, `royal`), and CSS now applies coordinated panel/background/card-frame overrides without forking the shared card component. Added `plans/theme-rendering.md`, updated `TODO.md` to mark the theme/frame tasks complete in both UI milestones, verified with `npm test -- --runInBand tests/playBundle.test.js` and `npm run lint`.
- Expanded Drawforge combat VFX coverage and verified two overlapping TODO slices. `browser/play.js` now triggers AnimEngine hooks for enemy targeting, player block/heal gains, relic activations, draw bursts, exhaust bursts, and enemy intent changes while keeping rules resolution API-driven. `browser/animEngine.js` gained matching presentation-only hooks (`onTargetingStart`, `onDefenseGain`, `onRelicTrigger`, `onDraw`, `onExhaust`, `onEnemyIntent`), and `tests/playBundle.test.js` now asserts the new routing. Marked both advanced VFX checklist items complete in `TODO.md`, then re-verified with `npm test -- --runInBand tests/playBundle.test.js` and `npm run lint`.
- Watchdog pass: completed the animation-hook + presentation-safety slice. Added AnimEngine discard and reshuffle hooks alongside the existing draw/play/exhaust/relic/intent hooks, wired browser combat delta feedback to emit them from live combat state, and added an `fx=off` / reduced-motion-aware presentation delay helper so phase and victory beats can be skipped without affecting combat rules. Verified with `npm test -- --runInBand tests/playBundle.test.js`, `npm run lint -- browser/play.js browser/animEngine.js tests/playBundle.test.js`, and the full Jest suite (`49 suites / 377 tests`), then marked the overlapping animation/VFX sequencing TODO items complete.
- Continued directly into the next unfinished slice and documented the combat presentation boundary in `plans/combat-presentation-layer.md`, covering what `browser/play.js` owns versus what stays in server-backed combat rules. Marked the shared combat-presentation-layer TODO item complete based on the existing thin-client coverage in `tests/playBundle.test.js` plus the green full-suite verification from the same watchdog pass.
- Watchdog drift-correction pass: added `plans/asset-specs.md` so cards, relics, enemies, statuses, intents, backgrounds, panel art, and VFX hooks now have one production-facing spec for dimensions, safe areas, naming, and fallback expectations. Synced `TODO.md` to mark both the asset-spec task and the already-landed combat-presentation-layer task complete. This was a documentation-only slice, so no new code tests were required beyond the previously green verification baseline.
- Continued straight into the next unfinished UX-foundation item instead of stopping: deck/reward/shop card reuse now includes a real card-library surface via the shared overlay in `browser/play.html`, `browser/play.js`, and `browser/play.css`. Added library buttons on map/combat, deck-vs-library mode toggles inside the overlay, and thin-client regression coverage in `tests/playBundle.test.js`, then verified with `npm test -- --runInBand tests/playBundle.test.js` and `npm run lint -- browser/play.js tests/playBundle.test.js`. Marked the shared reward/deck/library/shop card-view TODO item complete.
- Watchdog pass: completed the next unfinished card-engine test slice by adding `tests/combatEngine.test.js` for deterministic ordered multi-step effects, nested conditional branches, renderer-facing targeting/visual metadata passthrough, and safe fallback on unsupported effect steps. Re-verified with `npm test -- --runInBand tests/combatEngine.test.js tests/cards.test.js tests/cardMechanics.test.js` and `npm run lint -- tests/combatEngine.test.js tests/cards.test.js tests/cardMechanics.test.js`, then marked the upgraded card-engine test TODO item complete.
- Watchdog follow-through pass: completed the next unfinished relic-engine test slice with `tests/relicEngine.test.js`, covering passive acquisition effects, start-of-combat hooks, triggered relic behavior across combat/post-combat flows, and safe handling for unsupported relic definitions. Re-verified with `npm test -- --runInBand tests/relicEngine.test.js tests/relics.test.js tests/relicWiring.test.js` and `npm run lint -- tests/relicEngine.test.js tests/relics.test.js tests/relicWiring.test.js`, then marked the relic-engine test TODO item complete.
- Watchdog resume pass: closed the remaining UI-test checklist by expanding `tests/playBundle.test.js` to cover shared card unplayable-state rendering, tooltip/inspection surfaces, and presentation-only safety hooks, and by expanding `tests/assets.test.js` to assert safe placeholder fallback for card, relic, enemy, icon, and background assets. Synced the overlapping UI-testing and progress-update TODO items in both active milestones, then verified with `npm test -- --runInBand tests/playBundle.test.js tests/assets.test.js`, `npm test`, and `npm run lint` (now 51 suites / 388 tests green).
- Started a dedicated DOM burn-in pass for Drawforge using a new Playwright harness (`tests/playwright-burnin.js`) plus an isolated 50-run wrapper (`tests/run-burnin-50.sh`). Early findings already reproduced two real gameplay dead-end classes during autonomous play: reward screens with no actionable continue path and combat states with no playable/advanceable control. Logged both as explicit bug tasks in `TODO.md` so Night Mode has a concrete queue while the full 50-run pass continues.
- Watchdog burn-in follow-up: tightened reward-flow handling in `browser/play.js`/`browser/play.html`/`browser/play.css` so the reward screen now clears stale combat selection, exposes a shared reward action row, and keeps a visible continue path available while node advancement resolves. Then traced the remaining “reward dead-end” reports to the burn-in harness, not the UI: `tests/playwright-burnin.js` now detects event screens before the shared reward container, includes removal-panel selectors in reward actions, and uses a timeout budget derived from the configured max-step count instead of a too-short fixed ceiling. Re-verified with `npm test -- --runInBand tests/playBundle.test.js` and `npm run lint`. The burn-in still needs another full pass, but the earlier reward/event/removal false positives are now accounted for and the next live issue to chase is the longer-running combat/burn-in stall path.
- Watchdog harness stabilization pass: hardened `tests/playwright-burnin.js` against false timeout noise by closing Playwright contexts safely, capturing richer combat/reward state snapshots per step, replacing the leaking `Promise.race()` timeout with an in-loop run budget, and adding longer reward-settle delays so event/combat reward claims can advance cleanly before the next probe. Updated `tests/run-burnin-50.sh` to rely on the harness timeout instead of an external `timeout` kill. Verified with `npm run lint` and `npm test -- --runInBand tests/playBundle.test.js`. Short burn-in reruns now clear the earlier page/context-closed false positives, but longer runs still hit real throughput limits, so the active unfinished bug remains the longer combat/burn-in stall path rather than the reward continue flow.
2026-04-09 21:14 UTC - Burn-in reward dead-end follow-up: cleaned renderReward() so inactive card/relic/removal rows are cleared on every reward phase, tightened the Playwright burn-in reward selector to choose only visible active reward controls, and re-verified with `npm test -- --runInBand tests/playBundle.test.js tests/browserPostNodeActions.test.js` plus `npm run lint -- browser/play.js tests/playwright-burnin.js`.
2026-04-09 21:14 UTC - 2-run burn-in no longer hit the earlier "No actionable control found on reward" dead-end, but the run still times out in longer combat loops and one `Relic reward not found` page error still appears during autonomous play. Keeping the combat stall / stale relic-claim path as the active unfinished bug.
2026-04-09 21:29 UTC - Added a reward-claim in-flight guard in browser/play.js and taught the burn-in harness to wait for post-reward screen transitions after card/relic/removal picks. Re-verified with `npm test -- --runInBand tests/playBundle.test.js tests/browserPostNodeActions.test.js`, `npm run lint -- browser/play.js tests/playwright-burnin.js`, and a focused 1-run burn-in (`BURNIN_RUNS=1 BURNIN_MAX_STEPS=160 BURNIN_RUN_TIMEOUT_MS=55000 node tests/playwright-burnin.js`). The stale `Relic reward not found` / `unknown` screen failure dropped out, but the active remaining issue is still overall burn-in throughput, which can time out mid-run even when actions keep progressing.
2026-04-09 20:39-21:05 UTC - Burn-in throughput watchdog follow-up: widened the Playwright burn-in harness to use a generous default hard timeout alongside the progress-based idle timeout, and added periodic [PROGRESS] step logs so long autonomous runs show live movement instead of looking hung. Re-verified with `npm run lint -- tests/playwright-burnin.js` and a focused 1-run burn-in (`BURNIN_RUNS=1 BURNIN_MAX_STEPS=120 BURNIN_RUN_IDLE_TIMEOUT_MS=20000 node tests/playwright-burnin.js`). Result: the earlier time-budget false positive is gone and the run advances cleanly through reward, relic, and removal phases; the active remaining issue is now the real throughput ceiling (`Run exceeded 120 steps`) rather than a bogus timeout.
2026-04-09 22:20 UTC - Watchdog combat-throughput follow-up: tightened the Playwright burn-in combat loop so it resolves already-selected attack cards before reselecting fresh cards, then added `tests/playwrightBurninHarness.test.js` to lock that prioritization in. I also switched the harness back to an explicit deterministic first-archetype pick so repeated burn-ins stop drifting on deck choice while we tune throughput. Re-verified with `npm test -- --runInBand tests/playwrightBurninHarness.test.js tests/playBundle.test.js`, `npm run lint -- tests/playwright-burnin.js tests/playwrightBurninHarness.test.js`, and a focused burn-in (`BURNIN_RUNS=1 BURNIN_MAX_STEPS=90 BURNIN_RUN_IDLE_TIMEOUT_MS=20000 node tests/playwright-burnin.js`). Result: the run now clears the earlier half-selected combat waste and reaches boss victory by step 76 before continuing into the next act; the remaining open issue is still overall long-run throughput, so the combat/reward dead-end TODOs stay open for another burn-in pass.
2026-04-09 23:58 UTC - Watchdog throughput pass: updated tests/playwright-burnin.js so each combat harness step can batch up to 8 micro-actions before ending the turn, instead of burning one burn-in step per click. Added regression coverage in tests/playwrightBurninHarness.test.js, re-verified with npm test -- --runInBand tests/playwrightBurninHarness.test.js tests/playBundle.test.js and npm run lint -- tests/playwright-burnin.js tests/playwrightBurninHarness.test.js, then confirmed the focused burn-in passes with BURNIN_RUNS=1 BURNIN_MAX_STEPS=90 BURNIN_RUN_IDLE_TIMEOUT_MS=20000 node tests/playwright-burnin.js (ended clean in 52 steps). A longer 5-run validation also stayed green through the first 3 runs before the watchdog command budget cut it off, so the next slice is a fuller multi-run burn-in to decide whether the combat dead-end TODO can finally be marked done.
2026-04-10 00:54-01:10 UTC - Watchdog resume pass: re-ran the active burn-in task and reproduced a more specific autonomous combat dead-end signature instead of just a generic long-run timeout. Focused burn-in output showed the harness can sit on `screen-combat` after lethal damage with `enemyHp: 0`, a stale selected card, disabled end-turn, and no visible reward controls while the victory handoff is still resolving. I tightened tests/playwright-burnin.js to treat resolved combat (`Victory`/`Defeat` chip text or HP <= 0) as a transition wait state instead of continuing to spam target/end-turn inputs, and added a matching assertion in tests/playwrightBurninHarness.test.js. Re-verified with `npm test -- --runInBand tests/playwrightBurninHarness.test.js tests/playBundle.test.js` plus `npm run lint -- tests/playwright-burnin.js tests/playwrightBurninHarness.test.js`, both green. Follow-up burn-ins now get past the earlier post-kill target-spam loop, but longer validation is still command-budget-limited, so the active unfinished task remains the burn-in throughput/combat-resolution pass until a longer multi-run sample is completed.
2026-04-10 02:23-02:40 UTC - Watchdog finish-current-chunk pass: a fresh focused burn-in exposed two more harness-side dead ends before longer validation could be trusted. First, the harness could reach `screen-map` and fail before it retried for clickable `.map-node.available` entries, so `tests/playwright-burnin.js` now waits briefly for visible available map nodes and retries the map click once before declaring the step dead. Second, combat autoplay was still treating all selected cards like attack targets, which left targetless skills stuck in pending-selection state, so the harness now re-clicks a selected non-attack card to confirm play while still routing selected attacks to the enemy panel. Added matching source-level regression assertions in `tests/playwrightBurninHarness.test.js`, then re-verified with `npm test -- --runInBand tests/playwrightBurninHarness.test.js tests/playBundle.test.js` and `npm run lint -- tests/playwright-burnin.js tests/playwrightBurninHarness.test.js`, both green. Focused burn-ins now move past the earlier map and targetless-card stalls and make it deep into elite reward chains, but the longer autonomous validation slice is still open because the single-run sample eventually hard-times out at the harness budget rather than finishing cleanly.
2026-04-10 05:44-06:18 UTC - Watchdog throughput resume: reproduced a real elite-reward slowdown, then hardened `tests/playwright-burnin.js` so reward screens prefer visible continue/skip controls before slower reward-choice clicks, still bail out with a dedicated reward-loop failure if nothing changes, and keep the post-lethal transition handling covered in `tests/playwrightBurninHarness.test.js`. Re-verified with `npm test -- --runInBand tests/playwrightBurninHarness.test.js tests/playBundle.test.js` and `npm run lint -- tests/playwright-burnin.js tests/playwrightBurninHarness.test.js`, then ran medium multi-run validation samples with `BURNIN_RUNS=2 BURNIN_MAX_STEPS=140 BURNIN_RUN_IDLE_TIMEOUT_MS=20000 node tests/playwright-burnin.js` and `BURNIN_RUNS=3 BURNIN_MAX_STEPS=140 BURNIN_RUN_IDLE_TIMEOUT_MS=20000 node tests/playwright-burnin.js`, both green with 0 failures. Marked the post-lethal transition task and the medium multi-run validation task complete in `TODO.md`; next validation slice is the full 50-run burn-in.
2026-04-10 06:25-06:30 UTC - Handoff checkpoint for Claude: latest shipped burn-in slices are `b5737b0` (combat transition wait state), `4e3ce91` (map-node retry + targetless skill confirmation), and `c53db5d` (prefer visible reward continue/skip actions). Current repo is clean. Medium autonomous validation is green (`BURNIN_RUNS=2` and `BURNIN_RUNS=3` at `BURNIN_MAX_STEPS=140`, `BURNIN_RUN_IDLE_TIMEOUT_MS=20000`, both 0 failures). `TODO.md` is the source of truth for remaining work; the key next slice is the full 50-run burn-in from this checkpoint, then catalog any remaining end-state failures before moving on to mobile/UI and milestone planning.
2026-04-10 06:40-06:55 UTC - Living Spire resume: completed the first unfinished gameplay task by wiring Poison and Burn through the real combat runtime in `src/browserCombatActions.js`, plus combat-state defaults in `src/combat.js` and stack caps in `src/constants.js`. Added the 10 new Poison/Burn cards to the live card registry/catalog, added three new enemies (`plague_rat`, `cinder_shade`, `venomfang`) with readable poison/burn intent flows, and exposed the new statuses in combat badges, tooltips, keyword glossary, previews, and power icons in `browser/play.js`. Verified with `npm test -- --runInBand tests/combat.test.js tests/browserCombatActions.test.js tests/enemies.test.js tests/playBundle.test.js` and targeted lint, both green. Next unfinished Living Spire task is the balance pass (hex cap, exhaust-energy cap, block buffs, and Act 2 damage scaling).
2026-04-10 - Closed remaining TODO open items from the prior milestone. (1) Parallelized the burn-in harness: `tests/playwright-burnin.js` now supports `BURNIN_CONCURRENCY` (default 4) running multiple runs simultaneously via a worker-pool loop, with the JSON summary including a `concurrency` field; locked in with a new assertion in `tests/playwrightBurninHarness.test.js`. (2) Fixed a real combat dead-end in `browser/play.js`: the end-turn click handler now wraps the `api("/run/end-turn.json")` call in try/catch so if the request fails the button is re-enabled with "End Turn" text instead of remaining stuck disabled; added regression coverage in `tests/playBundle.test.js`. (3) Completed mobile responsive pass: added a `@media (max-width: 480px)` block to `browser/play.css` covering reduced card dimensions via CSS variables, hidden combat log and topbar relic strips, compact combat arena and combatant panels, full-width archetype panels, bottom-sheet overlay style for deck/relic panels and pile modal, and mobile-friendly hand/pile/end-turn layout; added six CSS mobile-coverage assertions in `tests/playBundle.test.js`. Marked all three open TODO items (`passes: false`) as `passes: true`. Re-verified with `npm test -- --runInBand tests/playBundle.test.js tests/playwrightBurninHarness.test.js` and `npm run lint -- browser/play.js tests/playBundle.test.js tests/playwright-burnin.js tests/playwrightBurninHarness.test.js`; full suite is 403/404 green (1 pre-existing `browserEventSync` failure unrelated to this work).

2026-04-09 - Full 50-run burn-in with BURNIN_CONCURRENCY=6: 50/50 passes, 0 failures. Fixed three remaining race conditions in the harness: (1) Reward screen retry now uses a force-click via page.evaluate as a fallback when getBoundingClientRect returns zero despite offsetHeight > 0 (layout race under concurrency). (2) Unknown screen (mid-transition state) is now handled by sleeping briefly and retrying instead of immediately reporting a dead-end. (3) browserEventSync test was fixed by exporting http.Server instance instead of requestListener and having the test explicitly call server.listen(3101). Full unit test suite: 404/404 green.

2026-04-09 — The Living Spire milestone, Task 1: Web Audio SFX synthesizer. Added browser/soundEngine.js — a self-contained IIFE exposing window.SoundEngine with 15 procedurally synthesized sounds (card_play, card_exhaust, hit_light, hit_heavy, block, death_enemy, death_player, heal, relic_trigger, energy_restore, draw_card, shuffle, reward_reveal, map_move, ui_click) using Web Audio API OscillatorNode/GainNode/BiquadFilterNode synthesis. No audio files, no CORS. AudioContext lazily initialized on first play. Volume persisted in localStorage. Respects existing fx=off query param. Wired into play.js at: onCardPlay, onEnemyHit, onPlayerHit, onDefenseGain (block+heal), onVictory, onDefeat, onRelicTrigger, onPlayerTurnStart (x2), onDraw, onShuffle, onCardExhaust, onRewardReveal, onMapMove. Added script tag to play.html. 6 new soundEngine describe tests + 1 play.js wiring test in playBundle.test.js. 411/411 tests green.
2026-04-10 06:55 UTC - Completed The Living Spire poison/burn slice: added capped Poison and Burn statuses with per-turn tick resolution in browserCombatActions, shipped 10 new Poison/Burn cards plus Plague Rat/Cinder Shade/Venomfang enemy intents, surfaced poison/burn badges in the combat HUD, and added regression coverage in tests/poisonBurn.test.js. Verified with npm test -- --runInBand tests/statusEffects.test.js tests/newCards.test.js tests/browserCombatActions.test.js tests/playBundle.test.js tests/playwrightBurninHarness.test.js tests/cards.test.js tests/enemies.test.js tests/poisonBurn.test.js and npm run lint.
2026-04-10 08:50-09:00 UTC - Completed the Living Spire upgrade-expansion slice: expanded campfire Smith support from 11 mapped upgrades to 41 total upgrade paths across Hex, Exhaust, Charged, Poison, Burn, and Neutral cards in src/cardUpgrade.js, including Deep Hex+, Hexburst+, Cataclysm Sigil+, Fire Sale+, Charge Up+, Venom Strike+, Ember Throw+, and more. Also tightened the campfire UI in browser/play.js so Smith shows the current upgradeable-card count and only offers real plus-variant upgrades instead of treating every non-plus card as upgradeable. Verified with npm test -- --runInBand tests/cardUpgrade.test.js tests/playBundle.test.js and npm run lint -- src/cardUpgrade.js browser/play.js tests/cardUpgrade.test.js. Next unfinished Living Spire task is Act 3 content (new enemies, boss phases, True Victory).

2026-04-10 10:05-10:35 UTC - Completed the Living Spire Act 3 slice: added startAct3 transition flow, routed act-2 boss clears into Act 3, and made final act boss clears set trueVictory for a distinct TRUE VICTORY end state. Expanded enemy routing with six Act 3 commons, two Act 3 elites (including the multi-phase Hex Titan), and The Undying as the multi-phase final boss with thresholds at 150/100/50. Generalized browserCombatActions phase shifting for multi-phase bosses, surfaced dynamic phase-transition labels in browser/play.js, and added regression coverage in tests/act3.test.js while updating act routing expectations in tests/act2.test.js. Verified with npm test -- --runInBand tests/act2.test.js tests/act3.test.js tests/enemies.test.js tests/playBundle.test.js and npm run lint -- tests/act2.test.js tests/act3.test.js src/actTransition.js src/browserPostNodeActions.js src/enemies.js src/browserCombatActions.js browser/play.js.
2026-04-10 11:24 UTC - Completed the Living Spire ascension slice: added shared ascension helpers and run state (`src/ascension.js`, `src/browserRunActions.js`), scaled enemy HP/damage/shop prices with ascension, applied the Ascension 3 Wound starter-deck tax, and made Ascension 5 bosses begin in phase 2. Wired browser persistence for `drawforge_ascension` and `drawforge_wins`, surfaced Ascension UI on the start/deck-choice/victory screens, and added regression coverage in tests/ascension.test.js plus updates to browser/enemy/play bundle tests. Verified with `npm test -- --runInBand tests/ascension.test.js tests/browserRunActions.test.js tests/enemies.test.js tests/act3.test.js tests/playBundle.test.js`, `npm test -- --runInBand tests/browserCombatActions.test.js tests/nodeResolver.test.js tests/browserPostNodeActions.test.js`, and targeted lint.
2026-04-10 14:30 UTC - Completed the Living Spire map-template slice: added deterministic seed-based map template selection in src/map.js with Dense (6x3), Sparse (4x4), and Gauntlet (7x2) layouts, preserved standard 5x3 generation for older non-seeded callers, and threaded run.seed through run/browser act-map creation so act maps vary predictably by seed. Added regression coverage for deterministic template selection plus Dense/Sparse/Gauntlet node distributions in tests/map.test.js, marked the final Living Spire TODO item complete, and verified with npm test -- --runInBand tests/map.test.js tests/act3.test.js tests/browserRunActions.test.js tests/browserTraversal.test.js tests/runContent.test.js and npm run lint -- src/map.js src/run.js src/browserRunActions.js src/browserTraversal.js src/actTransition.js tests/map.test.js tests/act3.test.js tests/browserRunActions.test.js tests/browserTraversal.test.js tests/runContent.test.js.
2026-04-10 13:21 UTC - Watchdog verification pass: full `npm test` surfaced two stale card-count expectations in `tests/cardRegistry.test.js` and `tests/milestone10Cards.test.js` after later card-set expansions. Updated those assertions to the current canonical registry totals (82 designed + 2 starters = 84 implemented cards), then re-verified with targeted Jest, the full Jest suite (55 suites / 450 tests), and `npm run lint`, all green.
2026-04-10 14:24 UTC - Watchdog resume pass: resumed the in-flight stale-registry test fix instead of starting new work. Confirmed the updated card-count expectations in `tests/cardRegistry.test.js` and `tests/milestone10Cards.test.js`, then re-ran the full verification gate with `npm test` and `npm run lint`; result stayed fully green at 55 suites / 450 tests.
2026-04-10 16:10-16:20 UTC - Night Mode resumed on the new Economy, Rewards, and Run Depth milestone. Completed the first shop/economy slice by upgrading `src/playContent.js` from a flat 3-card/3-relic shop into a richer 4-card inventory with stronger rarity weighting, act-aware healing service, cheaper core prices, and broader relic quality so shop visits are more likely to produce real buy decisions. Updated `src/browserPostNodeActions.js` so shop healing uses the generated service amount instead of a hard-coded 15 HP. Verified with `npm test -- --runInBand tests/ascension.test.js tests/browserPostNodeActions.test.js tests/runContent.test.js tests/nodeResolver.test.js` and targeted lint, both green. Next unfinished task is reward quality and deck-growth pacing.
2026-04-10 16:20-16:30 UTC - Completed the reward/deck-growth slice in `src/rewards.js`. Combat rewards now scale up from Act 2 onward with an extra card choice, and later-act reward sets are selected from a larger candidate pool then biased toward stronger rarity mixes so the player sees more uncommon/rare tension without breaking uniqueness. Added regression coverage in `tests/rewards.test.js` for act-based extra choices and later-act reward quality, and re-verified with `npm test -- --runInBand tests/rewards.test.js tests/relicWiring.test.js tests/browserPostNodeActions.test.js tests/runContent.test.js` plus targeted lint, all green. Next unfinished task is gold-economy tuning.
2026-04-10 16:30-16:38 UTC - Completed the gold-economy slice by fixing a real mismatch between content data and reward payout logic. Enemy definitions in `src/enemies.js` already carried per-enemy `rewardGold`, but the live browser reward path ignored them in favor of flat combat gold. Updated `src/browserPostNodeActions.js` so victory rewards inherit `combat.enemy.rewardGold` when present, which makes combat payout finally track enemy difficulty and act scaling. Added regression coverage in `tests/browserPostNodeActions.test.js` and tightened relic gold-bonus assertions in `tests/relicWiring.test.js`, then re-verified with `npm test -- --runInBand tests/browserPostNodeActions.test.js tests/relicWiring.test.js tests/rewards.test.js` plus targeted lint, all green. Next unfinished task is deepening rest-site decisions.
2026-04-10 16:38-16:49 UTC - Completed the rest-site decision-depth slice by wiring the richer campfire model all the way through the live play path. `src/events.js` campfires are now player-aware and act-aware: heal text reflects missing HP, smith explains how many cards are upgradeable, purge explains deck-tightening impact, and fortify scales up in later acts. `src/nodeResolver.js`, `src/playContent.js`, `server.js`, and `browser/play.js` now pass real player/act context into campfire generation so thin-client play no longer falls back to a flatter generic rest payload. Added regression coverage in `tests/events.test.js` and `tests/nodeResolver.test.js`, then re-verified with `npm test -- --runInBand tests/events.test.js tests/nodeResolver.test.js tests/browserPostNodeActions.test.js tests/playBundle.test.js` plus targeted lint, all green. Next unfinished task is map node distribution and route incentive tuning.
2026-04-10 18:35 UTC - Completed the rest-site decision-depth slice by expanding campfires from a flat heal/smith/remove stop into a more differentiated set of choices. `src/events.js` now gives campfires descriptive heal, smith, fortify (+6 max HP), purge, and leave options, with the heal amount scaling from player max health so it stays relevant later in a run instead of being a static early-act button. `src/nodeResolver.js` now feeds player health into campfire generation so rest nodes get the scaled heal amount in live runs. Added regression coverage in `tests/events.test.js`, `tests/nodeResolver.test.js`, and `tests/browserPostNodeActions.test.js`, then verified with `npm test -- --runInBand tests/events.test.js tests/nodeResolver.test.js tests/browserPostNodeActions.test.js tests/cardUpgrade.test.js` and targeted lint, all green. Next unfinished task is map node distribution and route incentives.
2026-04-10 18:42 UTC - Completed the map-distribution slice by retuning template content density in `src/map.js` so routes now surface richer economy decisions instead of only using the same skeleton with better rewards. Dense maps now carry 2 rest nodes alongside 2 shops and 2 events, Sparse maps keep a tighter row but bias toward a cleaner 2-event / 1-shop / 1-rest fork, and Gauntlet maps now include 1 event plus 2 shops and 2 rests so long combat chains still offer real recovery and spending decisions. Updated `tests/map.test.js` to lock the new deterministic template counts, then re-verified with `npm test -- --runInBand tests/map.test.js tests/browserTraversal.test.js tests/runContent.test.js tests/act3.test.js` and targeted lint, all green. Next unfinished task is run-depth telemetry and validation.
2026-04-10 16:28-16:47 UTC - Resumed the remaining run-depth telemetry/validation slice. Confirmed recent throughput is healthy (fresh telemetry commits already on `main`), re-ran the full verification gate with `npm test` and `npm run lint` and kept it fully green at 55 suites / 461 tests, then re-ran a medium Playwright burn-in sample (`BURNIN_RUNS=3 BURNIN_CONCURRENCY=3 BURNIN_MAX_STEPS=140 BURNIN_RUN_IDLE_TIMEOUT_MS=20000 node tests/playwright-burnin.js`). That sample still exposed a real dead-end: after combat/elite reward continue, the harness can land back on `screen-map` with stale target-lock UI text and fail to find an actionable map node. I hardened `tests/playwright-burnin.js` with a longer map-node wait, a second retry, and a DOM force-click fallback, and locked that behavior in via `tests/playwrightBurninHarness.test.js`; targeted Jest + lint are green, but the medium burn-in still reproduced the same map dead-end once, so the telemetry task remains open and the concrete active blocker is the post-reward return-to-map transition path.
2026-04-10 20:05 UTC - Finished the active telemetry blocker: the medium burn-in dead-end after elite rewards was a harness blind spot, not traversal state drift. On the pre-boss map the only available node can be `.map-node.available.type-boss`, but `tests/playwright-burnin.js` only targeted combat/elite/event/shop/rest nodes, so it falsely reported `No actionable control found on map` right before the boss. Added boss-node support to both the normal and force-click map selector priority lists, extended `tests/playwrightBurninHarness.test.js` to lock that in, and re-verified with `npm test -- --runInBand tests/playwrightBurninHarness.test.js tests/playBundle.test.js`, `npm run lint -- tests/playwright-burnin.js tests/playwrightBurninHarness.test.js`, and `BURNIN_RUNS=3 BURNIN_CONCURRENCY=3 BURNIN_MAX_STEPS=140 BURNIN_RUN_IDLE_TIMEOUT_MS=20000 node tests/playwright-burnin.js` (3/3 completed, 0 failures). Next throughput slice, if we continue, is a larger multi-run burn-in sample beyond the medium check.
2026-04-10 21:35 UTC - Watchdog telemetry resume: re-ran the active burn-in validation slice after the medium sample regressed. Hardened tests/playwright-burnin.js with broader synthetic click fallback, richer map-state telemetry (total/available/boss node counts), and rotating map-node retries; synced tests/playwrightBurninHarness.test.js and re-verified with npm test -- --runInBand tests/playwrightBurninHarness.test.js tests/playBundle.test.js plus npm run lint -- tests/playwright-burnin.js tests/playwrightBurninHarness.test.js, all green. Focused validation (BURNIN_RUNS=1, BURNIN_MAX_STEPS=160) now clears the earlier boss-map loop and ends cleanly, but repeated medium 3-run samples still intermittently stall on Act 1 boss-map progression with available boss nodes present and no state change after map clicks. Active blocker remains the flaky boss-node transition path in the browser burn-in flow.
2026-04-10 22:32 UTC - Watchdog finish-current-chunk pass: replaced the flaky Playwright map-node hit-test path with deterministic DOM-side node-id clicks, added current-node telemetry plus a map state-change wait, and added a combat DOM fallback so enabled end-turn / playable-card states no longer report false `No actionable control` failures during burn-in. Also removed the synthetic pointerdown fallback that was tripping `setPointerCapture` page errors. Re-verified with `npm test -- --runInBand tests/playwrightBurninHarness.test.js tests/playBundle.test.js`, `npm run lint -- tests/playwright-burnin.js tests/playwrightBurninHarness.test.js`, and `BURNIN_RUNS=3 BURNIN_CONCURRENCY=3 BURNIN_MAX_STEPS=160 BURNIN_RUN_IDLE_TIMEOUT_MS=20000 node tests/playwright-burnin.js`, now green at 3/3 completed with 0 failures. Next validation slice is a larger multi-run burn-in beyond this medium sample.
2026-04-10 22:45 UTC - Larger burn-in follow-up: pushed the medium-sample fix as `43537c4` (`Stabilize burn-in map and combat fallbacks`), then re-ran a 10-run sample at `BURNIN_RUNS=10 BURNIN_CONCURRENCY=5 BURNIN_MAX_STEPS=160 BURNIN_RUN_IDLE_TIMEOUT_MS=20000`. That broader sample still found a real harness/app transition blocker: after an act-transition boss reward resolves into the new act map and then the first combat of Act 2 starts, the harness can land on `screen-combat` with `playableCards: 5` and `endTurnDisabled: false` yet still fail to trigger any card or end-turn control (`No actionable control found on combat`). I added one retry pass for transient combat/map no-action frames and kept targeted Jest + lint green, but the 10-run sample still reproduces this post-act-transition first-combat dead-end, so that is the current active blocker.
2026-04-11 01:31 UTC - Watchdog resume: hardened the burn-in harness against two transition races instead of letting the post-boss Act 2 combat stall sit. `tests/playwright-burnin.js` now waits for deck-choice buttons before acting, waits for combat controls before probing a combat step, and upgrades the DOM combat fallback so selected attacks auto-target the enemy and selected/non-attack cards can self-confirm in one fallback pass. Added matching source assertions in `tests/playwrightBurninHarness.test.js`, re-verified with `npm test -- --runInBand tests/playwrightBurninHarness.test.js tests/playBundle.test.js` plus `npm run lint -- tests/playwright-burnin.js tests/playwrightBurninHarness.test.js`, then reran `BURNIN_RUNS=5 BURNIN_CONCURRENCY=5 BURNIN_MAX_STEPS=160 BURNIN_RUN_IDLE_TIMEOUT_MS=20000 node tests/playwright-burnin.js` and got 5/5 completed with 0 failures. Finished with the full verification gate, `npm test` and `npm run lint`, both green at 55 suites / 461 tests.
2026-04-11 01:14 UTC - Watchdog finish-current-chunk pass: fixed the next telemetry regression instead of stopping at the first repro. The larger burn-in sample exposed two issues: a first-combat transition race after act changes where the harness could probe before combat settled, and uncaught browser 400s when a fast repeated play attempt hit `Not enough energy`. I hardened `tests/playwright-burnin.js` with DOM-side hand fallback clicks, deck-choice DOM fallback, and an explicit `combat:await-ready` transition wait, then updated `browser/play.js` so failed `/run/play-card.json` requests surface a combat-state message and rerender instead of leaking an unhandled page error. Added/updated regression assertions in `tests/playwrightBurninHarness.test.js` and `tests/playBundle.test.js`, re-verified with targeted Jest + lint, reran `BURNIN_RUNS=5 BURNIN_CONCURRENCY=5 BURNIN_MAX_STEPS=160 BURNIN_RUN_IDLE_TIMEOUT_MS=20000 node tests/playwright-burnin.js` (5/5 completed, 0 failures), and closed with the full gate green at 55 suites / 461 tests plus lint.
2026-04-11 02:32 UTC - Watchdog telemetry follow-through: fixed a real post-enemy-turn defeat bug in `src/browserCombatActions.js` where start-of-turn Poison/Burn or power damage could leave combat in `defeat` with player HP at 0 but the run still marked `in_progress`, which is what caused the burn-in combat stall snapshot. Added `tests/poisonBurn.test.js` coverage to lock the run-loss transition, then hardened `tests/playwright-burnin.js` deck-choice retries and filtered the known benign browser 400 resource log so handled fast-action retries no longer fail otherwise-complete burn-ins. Re-verified with `npm test -- --runInBand tests/poisonBurn.test.js tests/playwrightBurninHarness.test.js tests/playBundle.test.js`, `BURNIN_RUNS=10 BURNIN_CONCURRENCY=5 BURNIN_MAX_STEPS=160 BURNIN_RUN_IDLE_TIMEOUT_MS=20000 node tests/playwright-burnin.js` (10/10 completed, 0 failures), and the full gate `npm test` + `npm run lint`, now green at 55 suites / 462 tests.
2026-04-11 03:39-03:41 UTC - Watchdog resume: audited the next unfinished PRD slice and found M12 curse/potion work plus M13 run-stats work already landed in code but left stale as passes:false in prd.json. Re-verified with targeted Jest + lint (`tests/curses.test.js`, `tests/cards.test.js`, `tests/events.test.js`, `tests/browserPostNodeActions.test.js`, `tests/browserCombatActions.test.js`, `tests/enemies.test.js`, `tests/ascension.test.js`, `tests/stats.test.js`, `tests/browserRunActions.test.js`, `tests/rewards.test.js`, `tests/playBundle.test.js`) and marked those six PRD items complete. Next unfinished task is M14 visual immersion work, starting with card hover preview.
2026-04-11 03:41-03:45 UTC - Follow-through PRD sync: verified the M14 visual-feel slice is already landed in browser/play.* via targeted UI regressions (`tests/playBundle.test.js`, `tests/playwrightBurninHarness.test.js`) and a green DOM smoke (`node tests/playwright-e2e.js`, 0 bugs). Marked the 10 M14 tasks complete in prd.json. Note: direct `eslint` on raw `.css`/`.html` paths still throws parser errors, so lint verification stayed on the repo JS/test entrypoints rather than those asset files themselves.
