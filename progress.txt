- Implemented startNewRun with default player state, starter deck, and initial run metadata. Added Jest coverage for initialization and no-combat/no-map selection state.
- Added card model, strike/defend definitions, and a minimal combat engine to execute card effects. Added Jest tests covering card creation and effect execution.
- Verified starter deck composition (5 Strike, 5 Defend) and added tests ensuring it is stored on new runs.
- Added deck zone state (draw/hand/discard) with deterministic shuffle injection, draw/reshuffle/discard behavior, and tests covering draw/removal/reshuffle edge cases.
- Added combat encounter initialization with active state, player-first turn, and basic player/enemy combat state. Added tests for encounter setup.
- Added energy system with turn start initialization and card play energy checks, plus unit tests for spending and rejection on insufficient energy.
- Added card play flow for attack cards: energy payment, effect execution, and hand removal into discard. Added test ensuring Strike reduces enemy health and costs energy.
- Added defense card play coverage: Defend grants block, spends energy, and leaves hand. Added unit test and marked PRD item complete.
- Added damage resolution that consumes block before health and clamps health at zero. Added tests for partial and full block absorption.
- Added next player turn start helper that clears temporary block and refreshes energy. Added tests and marked PRD item complete.
- Added player-to-enemy turn transition helper that switches turn state and starts enemy action phase. Added unit test and marked PRD item complete.
- Added deterministic enemy attack that applies damage and ends enemy turn. Added tests for damage and block interaction, marked PRD item complete.
- Added post-enemy turn flow to refresh energy, reset block, and draw a new hand. Added tests and marked PRD item complete.
- Added combat end-state checks for enemy defeat (victory), wired into card play and enemy actions. Added tests and marked PRD item complete.
- Added test coverage for combat defeat when player health reaches zero and marked PRD item complete.
- Added reward generation for combat victory with deterministic card options and deck addition helper. Added tests and marked PRD item complete.
- Added deterministic map generation with connected rows of combat nodes. Added tests for node counts, types, and connectivity, and marked PRD item complete.
- Added traversal helpers to list valid starting nodes and select a starting node. Added tests for valid selection and rejection of non-starting nodes.
- Added traversal movement helper enforcing connected-node movement. Added tests for valid and invalid moves and marked PRD item complete.
- Added node resolution to start combat encounters on combat nodes. Added tests and marked PRD item complete.
- Added deck inspection helper to list current deck contents and verified reward cards appear in the view. Added tests and marked PRD item complete.
- Added deck removal helper with tests for successful removal and invalid selection, and marked PRD item complete.
- Added save/load support in `src/save.js` with a small storage adapter boundary and JSON-based serialization. Added Jest coverage for saving player/deck/map/combat state, restoring saved runs, malformed JSON, incomplete save data, invalid combat state, and missing saves.
- Added a top-level `README.md` documenting the repo layout, architecture, module guide, and recommended next implementation path.
- Updated `prd.json` to mark the save/load tasks complete after implementation and tests.
- Added balance tuning hooks in `src/balance.js` and wired them into run initialization, turn energy, map generation, combat-node enemy health, reward option counts, and enemy attack damage. Added Jest coverage for default config values and override-based tuning behavior.
- Added a minimal browser shell under `browser/` with New Run / Save / Load / Clear Save controls and a raw run-state viewer backed by `localStorage`. Added `server.js` plus an `npm start` script for local browser testing, and added Jest coverage for browser-demo helper functions.
- Added browser traversal helpers in `src/browserTraversal.js` and wired the browser shell to generate a map, show valid starting/connected nodes, enter nodes, and create combat encounters when moving onto combat nodes. Added Jest coverage for map attachment, valid starting-node selection, connected-node listing, and invalid movement rejection.
- Added browser combat helpers in `src/browserCombat.js` and extended the browser shell with a minimal combat panel showing player/enemy stats, hand rendering, card-play actions, and end-turn resolution into an enemy attack and next player turn. Added Jest coverage for combat startup, card instantiation, indexed hand play, invalid card selection, and end-turn resolution.
- Expanded run content with deterministic enemy variants, elite and boss encounters, event nodes, and a simple relic system. Added core modules for enemies and relics, updated map generation/node resolution/reward logic, and added Jest coverage for enemy selection, relic effects, multi-type map generation, event resolution, and elite/boss reward scaling.
- Upgraded the browser shell from a thin state viewer into a playable test loop: varied node types, event choices, post-combat reward selection, relic tracking, gold progression, boss-win/loss states, and better in-browser run progression. Also added lightweight UI feedback polish through button transitions and status animation.
- Expanded the card/reward pool with `bash`, `barrier`, and `quick_strike`, and updated reward generation to draw from a more varied deterministic set of reward cards.
- Added a simple browser-side card removal flow that can be entered through certain event/reward states, so runs now support practical deck thinning in the test UI rather than only through backend helpers.
- Polished the browser combat/run UX with explicit enemy intent, draw/discard pile counters, reshuffle feedback in status text, and a dedicated end-state panel for victory/defeat so the run outcome is much clearer during manual testing.
- Cleaned up accidental duplicated garbage at the ends of `browser/index.html` and `browser/styles.css` caused by earlier edits, restoring those browser assets to a sane state.
- Added more interesting card effects with browser-usable `focus` (energy utility) and `volley` (attack + draw-style support in the browser model), while keeping the reward pool deterministic and test-covered.
- Added deterministic event variety through `src/events.js` with shrine, forge, and camp-style events instead of a single repeated event pattern. Wired event generation into node resolution and the browser shell.
- Refactored the browser app into a cleaner event/card loop with richer reward-card generation and multiple event outcomes, while keeping save/load and the existing run loop intact.
- Added utility/replacement cards `focus` and `volley`, extended both the browser card library and the core reward pool to include them, and added test coverage for their card definitions/effects.
- Added deterministic multi-template event generation in a dedicated `src/events.js` module and expanded event outcomes to include gold, reward-card offers, and add-card results in addition to healing/relic/removal choices.
- Added enemy behavior variety with deterministic intent sequences for normal, elite, and boss enemies. Enemy actions now support attack, multi-hit attack, block, and buff behaviors instead of every encounter being a flat single-hit attack.
- Updated the browser combat UI to display real readable enemy intent labels and verified through DOM automation that intents advance between turns (for example, Slime rotates from `Slam for 6` to `Harden: gain 6 block`).
- Added visible enemy block to the browser combat panel and fixed the UI wiring so DOM-based testing can read both current intent and current enemy block directly from the page.
- Improved enemy-action feedback in the browser by surfacing the resolved intent label in the status text after enemy turns (for example, after ending a turn against Slime the UI now reports `Slam for 6` instead of a generic message).
- Added a new mechanics layer for cards: exhaust-style one-shots (`surge`, `hex`), temporary energy gain, and a simple debuff interaction where `punish` deals bonus damage against hexed enemies.
- Extended the browser combat UI to track exhaust pile count and enemy hex stacks, and expanded the browser reward/event card pool so the new mechanics can appear in actual runs.
- Added core Jest coverage for advanced card mechanics, including exhaust-pile routing and bonus damage against hexed enemies.
- Fixed a browser rerender bug that made reward selection appear broken: the reward handler was working, but `renderCombat()` crashed because `combatEnemyHex` and `exhaustPileCount` were missing from the DOM elements map. After fixing those bindings, selecting a reward now clears the reward panel and updates the deck/state correctly.
